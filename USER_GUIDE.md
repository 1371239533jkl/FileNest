# 智能文件管家 — 使用文档

## 1. 简介

智能文件管家（FileNest）是一款本地 AI 驱动文件整理工具，帮助你自动扫描、分类、去重和管理文件。数据存储在 SQLite 嵌入式数据库中（项目根目录 `smart_file_manager.db`），零配置，无需联网，保障隐私安全。

**版本**：v2.4.0（批次 1-3 合并发布，基于 v2.0 轻量化重构）

## 2. 环境要求

- Python 3.9 或更高版本
- Windows / macOS / Linux 均可
- **无需安装 MySQL 或其他外部数据库**

## 3. 安装与启动

### 3.1 安装依赖

```bash
cd smart-file-manager
pip install -r requirements.txt
```

依赖清单（10 个包）：

| 包 | 用途 |
|----|------|
| PyQt6 | 桌面 GUI |
| Pillow | 图片处理 |
| PyPDF2 | PDF 属性提取 |
| PyMuPDF | PDF 页面渲染 |
| python-docx | Word 文档处理 |
| python-pptx | PPT 文档处理 |
| watchdog | 文件监控 |
| pygments | 语法高亮 |
| httpx | HTTP 客户端 |
| python-dotenv | 环境变量 |

### 3.2 配置（可选）

AI 功能需要 API Key：

```ini
# .env
SMART_FM_AI_KEY=你的API密钥
BOCHA_API_KEY=你的博查API密钥  # 可选，联网搜索用
```

其他功能无需任何配置。

### 3.3 启动

```bash
python main.py
```

首次启动自动在项目根目录创建 SQLite 数据库文件 `smart_file_manager.db`，包含全部 12 张数据表（含智能集合、清理排除/误报表）+ FTS5 全文搜索索引 + 默认设置，无需任何手动操作。

---

## 4. 界面导航

应用左侧为 52px 宽的**纯图标导航栏**，包含 10 个功能入口。鼠标悬停图标时显示 Tooltip 名称。

| 图标 | 功能 |
|------|------|
| 📊 | 仪表盘 |
| 📂 | 扫描管理 |
| 📁 | 分类管理 |
| 🔍 | 文件搜索 |
| 🤖 | AI 助手 |
| 📋 | 操作历史 |
| 🔄 | 回收区 |
| 🔃 | 重复文件 |
| 🏷 | 标签管理 |
| ⚙ | 系统设置 |

右上角有主题切换按钮（🌙 深色 / ☀️ 浅色）和版本号。

---

## 5. 功能说明

### 5.1 仪表盘

**用途**：可视化展示文件统计概况。

**统计卡片（第一行）**：
- 总文件数 / 已分类 / 标签数 / 存储用量
- 每个卡片带彩色图标和子标题（如覆盖率、剩余空间）

**中间行（三列）**：
- AI 文件洞察 — 需配置 AI Key，自动分析磁盘状况
- 类型分布饼图 — 图片/文档/代码/视频/音频/压缩包等
- 最近活动 — 显示最近的操作动态

**底部行**：
- 文件增长趋势 — 近 12 个月扫描文件数折线图
- 快捷操作 — 一键跳转到扫描、重复文件、AI 助手页面

> 点击"刷新"按钮重新加载所有数据。

---

### 5.2 扫描管理

**用途**：扫描指定目录，将文件信息录入数据库。

**操作步骤**：
1. 选择要扫描的目录
2. 勾选选项：
   - **递归扫描子目录**：扫描所有子文件夹
   - **计算文件哈希**：计算 SHA256 用于去重（大文件较慢）
   - **扫描后自动分类**：扫描完成后自动智能分类
   - **提取文件元数据**：图片 EXIF、PDF 属性等
   - **建立正文索引**：扫描完成后自动索引文本/PDF/DOCX 内容，供正文搜索使用
3. 点击"开始扫描"，等待进度条完成
4. 配置的目录显示在下方列表（可删除配置）

**增量扫描**：对已扫描过的目录自动跳过未变化的文件。

**自动增量落库**：启用文件监控后，文件的 新增/修改/移动/删除 会自动增量更新索引（事件风暴合并、单文件失败自动重试），无需手动重扫，并在底部提示同步结果。

**辅助入口**：
- **任务中心**：查看后台扫描 / 正文索引 / 清理分析任务的进度，可取消
- **索引检查**：发现失效路径与孤立正文索引，确认后安全修复
- **清理中心**：打开安全清理中心（见 5.9）

---

### 5.3 分类管理

**用途**：查看文件分类结果，执行批量操作。

**左侧分类导航**（树形）：
- 按类型：图片 / 文档 / 代码 / 视频 / 音频 / 压缩包 / 可执行文件 / 字体 / 其他
- 按日期：年-月格式
- 按关键词：工作 / 学习 / 生活 / 项目

**右侧文件列表**：
- 支持分页（默认 100 条/页）
- 选中文件时展开预览面板（图片缩略图 / 元数据 / 快捷操作）
- 右键菜单：打开文件 / 打开目录 / 复制路径 / 重命名 / 删除 / AI 描述

**批量操作**：多选文件后可批量重命名或批量移动。

**整理规则管理**：顶部工具栏可打开整理规则管理对话框，用 关键词 / 扩展名 / 正则 三类规则自定义自动分类，支持新增、编辑、删除、启停、优先级排序，并可输入文件名测试规则命中。

> 批量移动/删除/重命名会先生成**操作计划**（影响范围、冲突项、不可逆项、预计释放空间），确认后才执行；计划过期会自动重新校验。

---

### 5.4 文件搜索

**用途**：FTS5 全文搜索 + 多条件组合查找文件。

**搜索方式**：
1. **自然语言解读**（顶部输入框）：输入"近一年的文档"等，规则引擎自动解析（特殊字符容错，解析失败不崩溃，可点 ❓ 语法按钮查看示例）
2. **高级搜索**：文件名（FTS5 前缀匹配）/ 扩展名 / 文件类型 / 大小范围 / 重复状态 / 修改日期 / **搜索正文**（在已建立的内容索引中查找文本/PDF/DOCX，显示命中片段）
3. **智能集合**：常用搜索条件可保存为命名集合入库，之后一键复用
4. **AI 对话**：点击"💬 AI 对话"跳转到 AI 助手深度分析

结果分页显示，每页 100 条，支持 CSV 导出。右键菜单同分类管理。

---

### 5.5 AI 助手

**用途**：多轮对话式文件助手，支持工具自动调用。

**核心工具**：
- 📂 文件搜索 — 自然语言搜索本地文件
- 🌐 联网搜索 — 获取最新信息
- 📄 读取文件 — 分析和总结文件内容
- 🐍 代码执行 — 运行 Python 计算

**使用方式**：在对话框中描述需求，AI 自动选择合适工具执行。搜索结果附带"查看全部"按钮，一键跳转到搜索页查看完整表格。

**模型配置**：系统设置 → AI 模型 中添加和切换模型方案，支持自定义 API 端点。

---

### 5.6 操作历史

**用途**：查看和撤销文件操作。

记录的操作类型：scan / rename / move / classify / dedup / delete / restore。

| 操作 | 方式 |
|------|------|
| 单条撤销 | 点击操作行的"撤销"按钮 |
| 批量撤销 | 多选后点击底部按钮 |
| Ctrl+Z | 全局快捷键 |
| 筛选 | 按操作类型、日期范围、批次过滤 |

---

### 5.7 回收区

**用途**：管理已删除文件，支持恢复。

- 文件删除后先移动到 `.trash/` 目录，数据库标记为 deleted
- **恢复**：将文件放回原路径，数据库状态改回 active
- **永久删除**：从 `.trash/` 彻底移除，数据库标记为 purged（不可恢复）
- 支持分页（100 条/页）和批量操作

---

### 5.8 重复文件

**用途**：查找和清理重复文件。

检测采用**多阶段确认**：大小 → 快速哈希（文件头 64KB）→ 完整哈希 三级确认，未完整校验的不误判为重复；中断后可续算。未运行检测时列表为空。

**上方表格**：重复组列表（哈希值、副本数、单文件大小、浪费空间），分页每页 50 组。

**下方表格**：选中组的文件详情（名称、路径、大小、修改时间）。

**操作**：
- **🔎 多阶段重新检测**：执行三级确认检测
- **删除此副本**：移除单个副本，释放磁盘空间
- **保留最新副本**：按修改时间保留最新文件，其余移入回收区
- **AI 智能选择**：AI 分析组内文件，建议保留哪个副本

> 浪费空间 = (副本数 - 1) × 单文件大小；检测结果与安全清理中心的重复文件数据源一致。

---

### 5.9 安全清理中心

**用途**：聚合五类清理建议，一键安全清理。

从 扫描管理 页的 **清理中心** 按钮打开，聚合：重复文件 / 临时缓存 / 空文件 / 长期未用 / 超大文件，每项都附原因说明。

| 操作 | 说明 |
|------|------|
| 分类 Tab | 按类别浏览建议 |
| 全选当前页 | 选中当前分类页全部建议 |
| 移入回收区 | 将选中文件移入回收区（可撤销），行内按钮可逐项清理 |
| 排除目录 | 将选中文件所在目录加入排除，之后不再建议 |
| 误报反馈 | 将选中文件标记为误报，之后不再建议 |
| 排除管理 | 查看/清空排除目录列表 |
| 刷新 | 数据变化后手动重新分析 |

> 所有清理默认移入回收区（可撤销），不做物理删除。对话框单例复用 + 数据指纹缓存，打开秒开。

---

### 5.10 标签管理

**用途**：管理标签及文件关联。

**独立标签**：可创建不关联任何文件的标签。

**标签云**：所有标签按文件数量大小排列（字号/颜色自动变化）。

| 操作 | 方式 |
|------|------|
| 新建标签 | 输入框添加独立标签 |
| 点击标签 | 查看该标签关联的文件（分页 100 条/页） |
| 打标签 | 选中文件后添加标签（逗号分隔多个） |
| 移除标签 | 从文件中移除标签 |
| 删除标签 | 删除标签及所有关联 |

---

### 5.11 系统设置

**左侧分类**，右侧配置。

| 分类 | 内容 |
|------|------|
| 通用设置 | 日志级别、数据库文件路径/大小/引擎信息 |
| 扫描设置 | 递归扫描、包含隐藏文件、哈希算法、大文件阈值 |
| 重命名模板 | 自定义格式，实时预览 |
| 去重策略 | 保留最新/最旧/最短路径/手动选择 |
| AI 模型 | 新增/编辑/删除模型模板，连接测试，多后端切换 |

每页可独立保存或重置默认值。

---

## 6. 数据库说明

| 项目 | 说明 |
|------|------|
| 引擎 | SQLite（Python 标准库内置） |
| 文件 | `smart_file_manager.db`（项目根目录） |
| 模式 | WAL（Write-Ahead Logging），读写不互斥 |
| 索引 | FTS5 倒排索引（文件名/路径/扩展名） |
| 线程安全 | `threading.local()` 每个线程独立连接 |
| 缓存 | 8MB 页面缓存 + PRAGMA synchronous=NORMAL |

> 无需安装、无需配置、无需启动外部服务。删除数据库文件即可重置所有数据。

---

## 7. 常见问题

**数据库文件在哪？**
项目根目录的 `smart_file_manager.db`。注意不要手动删除正在使用的数据库文件。

**如何重置所有数据？**
删除 `smart_file_manager.db` 文件，重启应用自动重建。或运行 `python scripts/clear_all_data.py`。

**AI 功能无响应？**
① 确认 `.env` 中 `SMART_FM_AI_KEY` 已配置 ② 确认 `ai_models.json`（或 `ai_models.example.json`）中存在可用模型配置 ③ 在设置页执行"连接测试"。

**扫描后仪表盘无数据？**
点击仪表盘的"刷新"按钮重新加载。

**Windows 下界面模糊？**
应用已启用高 DPI 适配（`PassThrough` 策略），如仍模糊请检查 Windows 显示缩放设置。

**程序崩溃/报错怎么办？**
未捕获异常会自动写入 `logs/app.log`（含完整堆栈）并弹窗提示。反馈问题时请附上日志末尾的 ERROR 片段，便于快速定位。
